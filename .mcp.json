{
  "mcpServers": {
    "moodle-watch": {
      "command": ".venv/bin/python",
      "args": ["-m", "moodle_watch.server"],
      "env": {
        "MOODLE_WATCH_MIRROR": "${MOODLE_WATCH_MIRROR}"
      }
    }
  }
}
